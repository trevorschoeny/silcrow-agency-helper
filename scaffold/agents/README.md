# Roster — {project_name}

This file is the canonical list of agents in the organization. It is referenced from every other part of the project. Changes to the roster are significant and flow through the ADR process (see `docs/decision-process.md`).

---

## Current roster

| Agent | Tier | Directory | Reports to | Reports from |
|---|---|---|---|---|
| {user_role} | 0 | `agents/{user_dir}/` | — | {lead_role} |
| {lead_role} | 1 | `agents/{lead_dir}/` | {user_role} | {implementer_role} |
| {implementer_role} | 2 | `agents/{implementer_dir}/` | {lead_role} | — |
| Registrar | (outside) | `agents/registrar/` | — | anyone submitting proposals |

## Tier semantics

- **Tier 0 — {user_role}.** Strategic direction, existential decisions about the organization, HR-level concerns (creating, renaming, and retiring agents). Time horizon: months to years.
- **Tier 1 — {lead_role}.** Architecture, project-wide conventions, review of plans. Writes briefs, not specs. Time horizon: months.
- **Tier 2 — {implementer_role}.** Planning and execution under briefs. Time horizon: days to weeks.
- **Outside the hierarchy — Registrar.** Custodian of record integrity. Procedural authority only — never adjudicates the substance of decisions. Grows by fan-out (more registrars handling partitioned scopes), not by strata.

Each tier represents a qualitatively different kind of cognitive work with a longer time horizon than the tier below. See `docs/foundations/01-stratified-cognition.md` for the intellectual basis.

## Reporting lines

- The {lead_role} receives briefs from the {user_role} and escalates decisions that cross into strategic territory.
- The {implementer_role} receives briefs from the {lead_role}, drafts plans, executes approved plans, and escalates when briefs' assumptions don't hold or when execution would cross architectural boundaries.
- The Registrar receives proposals from any agent authorized to author them (currently, any agent in the roster), and reports procedurally on what they find.

## Adding, renaming, or retiring agents

Roster changes are architectural decisions. Do not edit this file freely.

The proper procedure:

1. The {lead_role} or {user_role} drafts an ADR proposing the change (template: `adr/_templates/madr-full.md`).
2. The ADR is submitted to `proposed/` with a message to the Registrar.
3. The Registrar validates form and assigns a §-number.
4. The {user_role} approves (or rejects) the ADR.
5. On acceptance, the Registrar updates this file and moves any affected agent directories.

Never delete an agent directory. If an agent is retired, archive their directory under `agents/_retired/` (the Registrar creates this directory on first use) and leave a pointer in this roster — agents' inbox archives are part of the historical record.

## References

- `docs/philosophy.md` — the design rationale for every tier and role in this roster.
- `docs/decision-process.md` — the process that governs roster changes.
- `adr/accepted/§0001-adopt-agent-org-scaffold.md` — the founding decision that created this roster.
